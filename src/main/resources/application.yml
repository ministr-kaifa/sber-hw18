spring:
  sql:
    init:
      mode: always
  output:
      ansi:
          enabled: always
  datasource:
      url: jdbc:postgresql://localhost:5432/postgres
      username: postgres
      password: postgres
      driver-class-name: org.postgresql.Driver